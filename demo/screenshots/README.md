# Demo screenshots

Capture these in PNG using the Windows Snipping Tool
(`Win + Shift + S`) and save them here using these exact names:

- `01-overview.png` — buggy C snippet, full output panel, LLVM Evidence visible
- `02-parse-errors.png` — invalid C, Parse errors tab populated
- `03-clang-missing.png` — Diagnostics still working, LLVM IR tab shows install hint
- `04-evaluation.png` — terminal output of `./evaluate.sh`

See the walkthrough in [../README.md](../README.md) for the exact
inputs and expected screen contents for each shot.
